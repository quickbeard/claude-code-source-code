// Stub for modifiers-napi native module
// This module is not available in external builds

export function isModifierPressed(_modifier: string): boolean {
  return false
}

export default { isModifierPressed }
